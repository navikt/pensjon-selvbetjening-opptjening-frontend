import React from 'react';
import { render, fireEvent } from '@testing-library/react';
import { OpptjeningView} from './OpptjeningView';

it('renders language selector and change language', () => {
    //let select = render(<OpptjeningView/>);

});
